pip install -r requirements.txt                                          # 1) 의존성 설치 (최초 1회)
python -c "from src.runner import run; run('samples/sample_admin.txt')"  # 2) 파이프라인 1회 실행 (CLI)
uvicorn web.app:app --reload                                             # 3) 웹 UI 실행 → http://localhost:8000
python -m pytest tests/ -q