# 법률 데이터셋 품질 개선 체크리스트

- 대상: `법률_dataset.csv` / `.json` / `unsloth_*.jsonl` (108 레코드)
- 소스: `2024590_의사국 의안과_의안원문.pdf` (건설산업기본법 일부개정법률안)
- 평가일: 2026-07-08
- **실무 활용 준비도: 약 55% (PoC·데모 가능 / 프로덕션 미달)**

## 요소별 점수 요약

| 평가 요소 | 점수 | 판정 | 근거 |
|---|---|---|---|
| 형식 완결성 | 100% | 우수 | 빈 필드 0건, 스키마 정합 |
| 학습 포맷 적합성 | 100% | 우수 | Alpaca·ChatML·ShareGPT 3종 모두 108줄 정합 |
| 중복 없음(고유 응답) | 99.1% | 우수 | 108건 중 107건 고유 |
| 질문 자연스러움 | 85.2% | 보통 | 제목 절단 의심 16건 |
| 데이터 다양성 | 54.7% | 미흡 | 소스 1건·카테고리 1종·instruction 4종의 카테시안 곱 |
| 사실 근거성(grounding) | 17.7% | 낮음(미검증) | 응답-원문 어휘 겹침 낮음, 자동 환각 검증 근거 부재 |
| 원문 품질(OCR) | 11.1% | 불량 | 108건 중 96건에 단어 중간 띄어쓰기 오류 |

> 가중치: 근거성 0.25 · 다양성 0.20 · 원문품질 0.15 · 형식/포맷/중복/질문 각 0.10.

---

## P0 — 프로덕션 투입 전 필수 (지금 착수)

### 1. OCR 원문 정제 (원문 품질 11% → 목표 90%+)  ✅ 반영
- [x] OCR 글자사이 공백("건 설 산 업") 제거 후처리 — `loaders._collapse_ocr_spacing`. 한 글자+공백 3연속 이상만 붙여 정상 단어 공백은 보존(오병합 방지). OCR 출력에 적용.
- [x] 검증: 회귀 테스트(`test_s_ocr_spacing_collapse`) — "건 설 산 업"→"건설산업", "검토 보고"·"미지 급" 보존.
- (참고) "미지 급"류 2글자+공백은 문서별 pypdf 추출 특성이라 안전 병합 규칙으로는 못 잡음 — 해당 원본 확보 시 개별 대응.

### 2. 질문 절단 수정 (질문 자연스러움 85% → 목표 98%+)  ✅ 이미 해결(PR#6)
- [x] `seg[:30]` 절단+조사 접미 로직 폐기 → LLM 자연어 질문 생성으로 교체. `_derive_question` 제거됨.
- [x] 재검수: 회귀 테스트(`test_s_question_not_truncated`) — 괄호 균형·`을(를)` 미노출.

### 3. 사실 근거성 검증 흔적 저장 (근거성 미검증 → 검증 가능화)  ✅ 반영
- [x] 레코드별 근거성 점수·플래그를 검증·JSON 메타데이터에 저장 — `validate._grounding`(output↔input 어휘 겹침), `metadata.grounding/grounded`.
- [x] `source_span`(원문 근거 구간=해당 청크) 필드 추가 → 감사 추적성.
- [x] 저근거 플래그(`grounded=false`, 임계값 `GROUNDING_MIN`)·평균 근거성/저근거 건수를 리포트에 명시.
- [x] 코드: `src/validate.py`, `src/export.py`.

## P1 — 학습 품질 향상 (다양성 확보)

### 4. 데이터 다양성 확대 (다양성 55% → 목표 80%+)
- [ ] 소스 문서 확대: 현재 1건 → 도메인 대표 문서 다수(다른 법안·판례·해설서)
- [ ] 카테고리 다변화: 현재 `knowledge` 단일 → Q&A·instruction·RAG 등 명세상 3형태 균형 배분
- [ ] instruction 템플릿 4종 고정 → 실제 업무 질의 패턴 반영해 표현·난이도 다양화
- [ ] 27개 청크 × 4템플릿 카테시안 곱 구조 탈피 (말투 과적합 위험 완화)

### 5. 응답 품질 심화
- [ ] "조건 → 처리" 응답이 원문에 실재하는 규칙인지 대조 (일반화·추정 표현 점검)
- [ ] 용어 정리 응답의 정의가 원문 정의와 일치하는지 표본 검수
- [ ] `output`이 `answer`와 100% 동일 → instruction형/Q&A형 산출물 차별화 검토

## P2 — 운영·재현성

- [x] 재생성 파이프라인 회귀 테스트: `python -m pytest tests/ -q` → 28 passed
- [x] `docs/status.md` 갱신 (품질 개선 반영)
- [x] 데이터셋 버전·검증 점수를 리포트(`dataset_report.md`)에 명시 — 버전 1.0·품질점수·평균 근거성·저근거 건수

---

## 목표 재평가 기준

P0 3개 항목(OCR·질문절단·근거검증) 완료 시 종합 준비도 **55% → 75%대**,
P1(다양성)까지 반영 시 **80%대** 도달 예상. 80% 이상을 프로덕션 파인튜닝·RAG 투입 기준선으로 권장.
